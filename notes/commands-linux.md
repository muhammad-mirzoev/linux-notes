# 🐧 Linux Commands & Notes

## 📁 Работа с файлами и директориями
- pwd              | показать текущий каталог
- ls               | вывести список файлов
- ls -la           | показать все файлы, включая скрытые
- cd <путь>        | перейти в каталог
- mkdir <имя>      | создать каталог
- rmdir <имя>      | удалить пустой каталог
- rm <файл>        | удалить файл
- rm -r <каталог>  | удалить каталог рекурсивно
- cp <файл1> <файл2> | копировать файл
- mv <файл1> <файл2> | переместить или переименовать файл
- touch <файл>     | создать пустой файл
- tree             | показать дерево каталогов
- stat <файл>      | подробная информация о файле

---

## 📄 Просмотр и редактирование файлов
- cat <файл>       | вывести содержимое файла
- less <файл>      | просмотр с прокруткой
- head <файл>      | первые строки файла
- tail <файл>      | последние строки файла
- nano <файл>      | редактирование в nano
- vim <файл>       | редактирование в vim
- wc -l <файл>     | количество строк
- diff <файл1> <файл2> | сравнение файлов

---

## 🔍 Поиск и фильтрация
- find <путь> -name <имя>     | поиск файла по имени
- grep <текст> <файл>         | поиск текста в файле
- grep -r <текст> <папка>     | рекурсивный поиск
- locate <файл>               | быстрый поиск файлов
- which <команда>              | путь к исполняемому файлу

---

## ⚙️ Система и процессы
- top                | мониторинг процессов
- htop               | улучшенная версия top
- ps aux             | список всех процессов
- kill <PID>         | завершить процесс
- kill -9 <PID>      | принудительное завершение процесса
- df -h              | информация о дисках
- du -sh <папка>     | размер папки
- free -h            | использование оперативной памяти
- uname -a           | информация о системе
- uptime             | время работы системы
- whoami             | текущий пользователь
- dmesg | tail       | последние системные сообщения

---

## 🔐 Права и пользователи
- chmod 755 <файл>              | изменить права доступа
- chmod +x <файл>               | сделать файл исполняемым
- chown <пользователь> <файл>   | сменить владельца
- chgrp <группа> <файл>         | сменить группу
- sudo <команда>                | выполнить команду от имени root
- adduser <имя>                 | добавить пользователя
- passwd <имя>                  | сменить пароль
- groups <пользователь>         | группы пользователя

---

## 🌐 Работа в сети
- ping <хост>                   | проверить соединение
- curl <url>                    | запрос к веб-странице
- wget <url>                    | скачать файл
- ifconfig / ip a               | информация о сетевых интерфейсах
- netstat -tuln                 | открытые порты
- ss -tuln                       | современная замена netstat
- ssh <user>@<хост>             | подключиться по SSH
- scp <файл> <user>@<хост>:<путь> | копировать файл по SSH
- rsync -avz <источник> <назначение> | синхронизация файлов

---

## 📦 Работа с пакетами
**Debian / Ubuntu:**
- sudo apt update
- sudo apt upgrade
- sudo apt install <пакет>
- sudo apt remove <пакет>

**Arch Linux:**
- sudo pacman -Syu
- sudo pacman -S <пакет>
- sudo pacman -R <пакет>

**Snap / Flatpak:**
- snap install <пакет>
- flatpak install <пакет>

---

## 🗜 Архивы и сжатие
- tar -cvf archive.tar <папка>       | создать архив tar
- tar -xvf archive.tar               | распаковать архив tar
- tar -czvf archive.tar.gz <папка>   | создать gzip архив
- tar -xzvf archive.tar.gz            | распаковать gzip архив
- zip -r archive.zip <папка>         | создать zip архив
- unzip archive.zip                   | распаковать zip архив

---

## 🧰 Разное и полезное
- history              | история команд
- clear                | очистить экран
- alias ll='ls -la'    | создать псевдоним
- date                 | текущее время и дата
- cal                  | календарь
- echo "текст"         | вывод текста
- uptime               | время работы системы
- watch <команда>      | запуск команды каждые 2 секунды
- basename <путь>      | имя файла без пути
- dirname <путь>       | путь без имени файла

---

# ZELIBOBA

---